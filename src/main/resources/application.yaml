spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/movieflix
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
   database-platform: org.hibernate.dialect.PostgreSQLDialect
   show-sql: true

  flyway:
    enabled: true

server:
  port: 8081

