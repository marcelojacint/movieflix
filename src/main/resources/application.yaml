spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:postgresql://<172.18.0.1>:5432/movieflix
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
   database-platform: org.hibernate.dialect.PostgreSQLDialect
   show-sql: true

  flyway:
    enabled: true
